# WeKnora 部署记录（Mac Studio 本地部署）

> **部署日期**：2026-08-24
> **部署目标**：在 Mac Studio 上部署 WeKnora 全套服务 + 本地 Ollama 模型，构建完全本地的私有知识库
> **状态**：✅ 部署成功，服务运行中

---

## 目录

- [1. 部署环境](#1-部署环境)
- [2. 部署结果](#2-部署结果)
- [3. 可复现的部署流程（干净版）](#3-可复现的部署流程干净版)
- [4. 踩坑与排障全记录](#4-踩坑与排障全记录)
- [5. 产物清单](#5-产物清单)
- [6. 后续操作指引](#6-后续操作指引)
- [7. 日常运维命令](#7-日常运维命令)

---

## 1. 部署环境

| 项目 | 配置 |
|---|---|
| 机器 | Mac Studio（Apple M2 Ultra，24 核，64GB 统一内存） |
| 系统 | macOS（Darwin 25.5.0） |
| Docker | Docker Desktop 29.7.2（linux/arm64 引擎，VM 内存 16GB） |
| Ollama | 0.32.15（宿主机运行，非容器） |
| WeKnora | v0.7.2（docker compose 标准部署） |
| 磁盘 | 部署前剩余 279GB |

**架构设计**：Ollama 跑在 macOS 宿主机（独占 GPU/统一内存），WeKnora 跑在 Docker，两者通过 `host.docker.internal:11434` 通信（WeKnora 默认配置即是此地址，零配置对接）。

---

## 2. 部署结果

### 容器状态（`docker compose ps`）

| 容器 | 镜像 | 大小 | 状态 | 说明 |
|---|---|---|---|---|
| WeKnora-app | wechatopenai/weknora-app:latest | 1.59GB | Up (healthy) | Go 后端（:8080） |
| WeKnora-docreader | wechatopenai/weknora-docreader:latest | 3.81GB | Up (healthy) | Python 文档解析（gRPC，仅容器网络内） |
| WeKnora-postgres | paradedb/paradedb:v0.22.2-pg17 | 1.83GB | Up (healthy) | ParadeDB（BM25 + 向量一体，不映射宿主机端口） |
| WeKnora-redis | redis:7.0-alpine | ~40MB | Up | 任务队列 |
| WeKnora-frontend | wechatopenai/weknora-ui:latest | 86.5MB | Up | nginx（:80） |

### 访问入口

| 服务 | 地址 |
|---|---|
| Web UI | http://localhost |
| 后端 API | http://localhost:8080（需认证，未登录返回 401 属正常） |
| Ollama | http://127.0.0.1:11434（宿主机） |

### 本地模型（Ollama）

| 模型 | 用途 | 大小 |
|---|---|---|
| qwen3.6:35b | 对话主模型（效果优先） | 23GB |
| qwen3.5:9b | 对话（速度优先） | 6.6GB |
| bge-m3 | **Embedding（知识库必需）** | 1.2GB |
| qwen3.8 | 下载中（完成后可随时添加/切换） | — |
| glm-ocr / gemma4:31b 等 | 原有模型，可选用于其他用途 | — |

---

## 3. 可复现的部署流程（干净版）

> 这是踩完所有坑后提炼的最短路径。**核心结论：网络受限环境下，镜像获取用"代理 + skopeo"通道，其余全用官方默认流程。**

### 前置

```bash
# Docker Desktop 已安装运行（建议 Resources 里把 VM 内存调到 12-16GB）
# Ollama 已安装并拉好模型：
brew install ollama && brew services start ollama
ollama pull qwen3.6:35b     # 对话模型（或其它）
ollama pull bge-m3          # embedding 模型（必需）
```

### 步骤

```bash
cd WeKnora

# ① 配置环境变量（替换默认密钥为随机值更安全）
cp .env.example .env
python3 -c "
import secrets, string, re
s = open('.env').read()
jwt = 'wk-' + ''.join(secrets.choice(string.ascii_letters+string.digits) for _ in range(40))
aes  = ''.join(secrets.choice(string.ascii_letters+string.digits) for _ in range(32))
s = re.sub(r'^JWT_SECRET=.*$', f'JWT_SECRET={jwt}', s, flags=re.M)
s = re.sub(r'^SYSTEM_AES_KEY=.*$', f'SYSTEM_AES_KEY={aes}', s, flags=re.M)
open('.env','w').write(s)"

# ② 安装 skopeo（宿主机镜像搬运工具）
brew install skopeo

# ③ 开启代理（全局模式），拉取全部镜像
./pull_images.sh http://127.0.0.1:7897 arm64 latest
#    拉完可切回规则模式

# ④ 启动
docker compose up -d

# ⑤ 验证
docker compose ps                          # 全部 Up/Healthy
curl -s -o /dev/null -w "%{http_code}" http://localhost   # 200

# ⑥ 浏览器打开 http://localhost → 注册首个账号（自动成为管理员）
#    模型设置 → Ollama（地址默认已对）→ 添加 qwen3.6:35b（对话）+ bge-m3（embedding）
```

---

## 4. 踩坑与排障全记录

这次部署的主要时间都耗在网络层，完整排障链路如下（按时间顺序），换环境遇到同类问题时可对照：

### 坑 0：Mac 上 Docker Desktop VM 默认内存只有 ~8GB

| 项 | 内容 |
|---|---|
| 现象 | 64GB 内存的机器，`docker info` 显示 Total Memory 仅 7.7GB——WeKnora 全家桶（docreader 含 LibreOffice/Playwright）会紧张 |
| 原因 | Mac 的 Docker 跑在虚拟机里，VM 内存**不自动用满宿主机**，默认给 8GB |
| 解法 | Docker Desktop → Settings → Resources → Memory 调到 12~16GB（本次事故后此改动已被回滚，**当前 VM 仍是默认值**，如遇内存紧张可按此调大） |

### 坑 1：`docker compose pull` 零进展

| 项 | 内容 |
|---|---|
| 现象 | pull 挂着不动，`docker system df` 大小零增长，流量采样 0.008MB |
| 排查 | 测各 mirror API 可达性（curl /v2/）→ 多数"通"；但 blob 下载全卡 |
| 结论 | 分层定位：**manifest API 通 ≠ blob 下载通**，二者走不同域名/链路 |

### 坑 2：mirror 免费源对 blob 限速

| 项 | 内容 |
|---|---|
| 现象 | 通过 mirror 前缀拉取，小图（alpine 3MB）30~40 秒下不完 |
| 排查 | 换 skopeo 在**宿主机**直接下载（排除 Docker VM 网络层嫌疑）→ 依然慢 |
| 结论 | **公共 mirror 免费源对 blob 下载限速（~30KB/s 量级）**，拉 6GB 需 40 小时，不可用 |

### 坑 3：代理分流规则掐死 Docker 引擎流量

| 项 | 内容 |
|---|---|
| 现象 | 开了系统代理，Docker 引擎 pull 更卡；`docker info` 显示引擎走 `http.docker.internal:3128`（Docker Desktop 内置转发器） |
| 排查 | 宿主机 curl 对比：经代理访问 mirror 38B/s（死）；绕过代理直连 0.24s（通） |
| 结论 | 代理软件（Clash 系，:7897）分流规则把 Docker 相关域名导向不可用线路。**关系统代理 + 重启 Docker Desktop** 可让引擎直连；但要走代理时需**全局模式** |

### 坑 4：Docker Desktop VM 卡死（最严重）

| 项 | 内容 |
|---|---|
| 现象 | 连续两次重启 Docker Desktop 后，引擎起不来：GUI 报 "An error occurred while loading the images list"，CLI 得到 500 |
| 原因 | 日志 `init failed to shutdown the VM: connection refused` —— Linux 虚拟机关闭失败卡在坏状态，进程全部空转 |
| 解法 | `pkill -f "Docker.app"` → `rm -f ~/.docker/run/docker.sock` → `open -a Docker` → 18 秒恢复，**镜像/容器数据无损**（数据在虚拟磁盘里，进程级操作不触碰） |

### 坑 5：镜像 tag 精确匹配问题（`up` 卡住的真凶）

| 项 | 内容 |
|---|---|
| 现象 | 镜像"都拉齐了"，但 `docker compose up -d` 迟迟不创建容器 |
| 排查 | `docker compose config --images` 发现 compose 要求 **`redis:7.0-alpine`**，而本地是 `redis:7-alpine` —— tag 不同 = 判定缺失 → 后台尝试拉取 → 卡死 |
| 解法 | `skopeo copy docker://redis:7.0-alpine docker-archive:...` 精确补拉 40MB 后 `up` 秒过 |
| 教训 | 拉镜像前先跑 `docker compose config --images`，**按输出清单精确拉取** |

### 坑 6：大文件下载中断流 / 收尾阶段假死

| 项 | 内容 |
|---|---|
| 现象 | ① weknora-app 下载到 105MB 时流量归零、进程空转（代理节点对长连接断流）；② tar 已写满（1552MB）但 10 秒零增量——实为 skopeo 收尾写 manifest 阶段，**假死误判** |
| 解法 | 下载脚本加 `timeout 900` 单次限时 + 失败自动重试 4 次（`pull_images.sh` 已内置）。注意 skopeo **无断点续传**，重试从头下载，好在已完成的镜像会跳过 |
| 教训 | 判断"卡死 vs 收尾"：看进程 CPU + 分两次采样文件大小，零增量且持续 >1 分钟才算真卡 |

### 坑 7：自写脚本测速 mirror API 不可靠

| 项 | 内容 |
|---|---|
| 现象 | 用 Python 按 Docker Hub 标准 auth 流程测各 mirror 的 blob 下载速度，全部 404/403 |
| 原因 | 各家 mirror 的 token 端点、路径、认证方式并不统一，标准协议假设不成立 |
| 解法 | **放弃自写脚本，统一用 skopeo**——它完整实现了各家 registry 的协议差异 |

### 最终通道（唯一成功路线）

```
✅ skopeo + HTTPS_PROXY(全局代理) + Docker Hub 官方源
   宿主机网络 → 代理节点 → registry-1.docker.io
   实测 6.5s/8.5MB，6GB 全量 20 分钟内完成
```

失败路线汇总：直连官方（不通）｜mirror + docker pull（限速）｜代理 + docker pull（分流规则坑）｜mirror + skopeo（同样限速）

---

## 5. 产物清单

| 文件 | 说明 |
|---|---|
| `.env` | 已配置（JWT_SECRET / SYSTEM_AES_KEY 已替换为随机值；Ollama 地址用默认 `host.docker.internal:11434`） |
| `pull_images.sh` | **可复用的镜像拉取工具**（代理 + skopeo + 超时重试 + 已有跳过），换服务器直接 `./pull_images.sh <代理> <架构> <版本>` |
| `部署记录.md` | 本文档 |

---

## 6. 后续操作指引

### 首次使用

1. 浏览器打开 `http://localhost`，注册首个账号（自动成为系统管理员）
2. **设置 → 模型管理**：确认 Ollama 供应商（默认地址已对接宿主机），添加：
   - 对话：`qwen3.6:35b`（qwen3.8 下载完成后同样方式添加/切换）
   - Embedding：`bge-m3`
3. **知识库**：新建 → 选 embedding 模型 → 上传文档（PDF/Word/Markdown 等）→ 等待解析完成
4. 提问验证：引用溯源、多轮对话、（可选）切换 Agent 模式

### 建议的双重玩法

- 64GB 统一内存下 Ollama 可同时驻留 30B 级模型 + embedding 模型，长期本地服务无压力
- 敏感文档用本地模型；追求极致回答质量时可另配云端 API（DeepSeek 等按量计费）——WeKnora 支持多供应商并存、按知识库指定模型

---

## 7. 日常运维命令

```bash
cd ~/DiaoYan/WeKnora

docker compose ps                    # 状态总览
docker compose logs -f app           # 跟踪后端日志（Ctrl+C 退出）
docker compose logs docreader        # 文档解析日志（排查解析问题）
docker compose restart app           # 重启单个服务
docker compose down                  # 停止全部（数据保留在卷里）
docker compose up -d                 # 再次启动
docker compose pull && docker compose up -d   # 升级版本（先改 .env 的 WEKNORA_VERSION）

ollama ps                            # 当前加载的模型
ollama list                          # 已下载的模型

# 数据备份要点：docker volume 里的 postgres-data（数据库+向量）与 data-files（原始文件）
docker volume ls | grep weknora
```

### 升级流程（官方支持）

```bash
# .env 中把 WEKNORA_VERSION 改为目标版本（如 v0.7.3），然后：
./pull_images.sh http://127.0.0.1:7897 arm64 v0.7.3   # 网络受限环境用脚本拉新版镜像
docker compose up -d                                   # 自动数据库迁移
```

---

*本文档记录 2026-08-24 的一次完整部署：从网络全堵到全套服务运行，总计耗时约 1.5 小时（其中约 1 小时耗在镜像获取的网络排障）。*
